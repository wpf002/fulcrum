// Embeddable buyer-intent tools (Phase 2): affordability calculator,
// rent-vs-buy, mortgage-readiness, neighborhood matcher.
//
// Ships as a standalone JS bundle an agent embeds on their site. On tool
// completion it captures consent inline (terms version + channel opt-ins)
// and POSTs a BuyerLeadSubmission to the api. No consent, no POST.

export {};
